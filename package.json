{
  "name": "mural-quest",
  "private": true,
  "scripts": {
    "cap:copy": "rsync -a --delete --delete-excluded --exclude node_modules --exclude ios --exclude android --exclude www --exclude .git --exclude .claude --exclude assets/wip --exclude '*.avif' --exclude '*.pdf' --exclude legacy-fixes --exclude __pycache__ --exclude images/murals --exclude images/thumbs --exclude js/content.json . www/",
    "cap:sync": "npm run cap:copy && npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.1",
    "@capacitor/cli": "^8.2.0",
    "@capacitor/core": "^8.2.0",
    "@capacitor/haptics": "^8.0.1",
    "@capacitor/ios": "^8.2.0",
    "@capacitor/keyboard": "^8.0.2",
    "@capacitor/status-bar": "^8.0.1",
    "@netlify/blobs": "^8.0.0",
    "stripe": "^17.0.0"
  }
}
